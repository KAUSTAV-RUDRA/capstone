"""Tests for src.calibration.conformal (scaffold stub). Serves Phase 3 §3.2.

Per-language thresholds; empirical FPR <= alpha per bucket (non-negotiable #3).
"""
from __future__ import annotations


def test_threshold_per_bucket_and_fpr_bound() -> None:
    """Placeholder: distinct threshold per bucket; empirical FPR <= alpha on test."""
    # TODO(phase-3 step-3.2): test per-bucket conformal thresholds + FPR bound.
    raise NotImplementedError
