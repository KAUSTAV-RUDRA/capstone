"""Tests for src.data.generate_machine_text (scaffold stub). Serves Phase 2 §2.1.3.

Held-out generators must never appear outside test (non-negotiable #4).
"""
from __future__ import annotations


def test_heldout_generators_are_test_only() -> None:
    """Placeholder: held-out-generator samples are routed to the test split only."""
    # TODO(phase-2 step-2.1.5): assert held-out generators never leak into train/cal.
    raise NotImplementedError
