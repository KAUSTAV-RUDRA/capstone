# mgt-detect dependencies (pinned).
# Install:  pip install -r requirements.txt
# Note: torch is pinned to the CPU-compatible release already on the dev box.
# For CUDA, install the matching torch build from https://pytorch.org first.

# --- Core ML / numerics ---
torch==2.6.0
transformers==4.49.0
accelerate==1.4.0
bitsandbytes==0.45.3          # 8-bit loading to respect the 4GB VRAM floor
scikit-learn==1.6.1
numpy==1.26.4
pandas==2.2.3
scipy==1.15.2

# --- NLP / multilingual / Indic ---
spacy==3.8.4
stanza==1.10.1
indic-nlp-library==0.92

# --- Config / utilities ---
pyyaml==6.0.2
tqdm==4.67.1

# --- Web layer (locked decision: Django + DRF, SQLite in dev) ---
django==5.1.6
djangorestframework==3.15.2

# --- Document parsing (analyse-page file upload) ---
python-docx==1.1.2
pypdf==5.3.0

# --- Plotting (risk-coverage curve F1, feature-distribution figures) ---
matplotlib==3.10.0
