# mgt-detect dependencies (pinned).
# Install:  pip install -r requirements.txt
# Note: torch 2.6.0 must come from the CUDA index, NOT PyPI - the PyPI wheel is
# CPU-only and every GPU part (generation, scoring, attacks) would silently run
# on CPU. After `pip install -r requirements.txt`, overwrite torch with:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu126
# (cu126 matches the dev box; pick the build for your driver at pytorch.org.)

# --- Core ML / numerics ---
torch==2.6.0
transformers==4.49.0
accelerate==1.4.0
bitsandbytes==0.45.3          # 8-bit loading to respect the 4GB VRAM floor
scikit-learn==1.6.1
numpy==1.26.4
pandas==2.2.3
scipy==1.15.2

# --- NLP / multilingual / Indic ---
spacy==3.8.4
stanza==1.10.1
indic-nlp-library==0.92

# --- Data sources / corpus building (src/data/build_human_corpus.py) ---
datasets==5.0.1               # HF streaming loaders (wikimedia/wikipedia, ai4bharat/samanantar)
datasketch==2.0.0             # MinHash LSH near-duplicate filter
pyarrow==25.0.1               # parquet backend for datasets + results/scores.parquet

# --- Config / utilities ---
pyyaml==6.0.2
tqdm==4.67.1

# --- Web layer (locked decision: Django + DRF, SQLite in dev) ---
django==5.1.6
djangorestframework==3.15.2

# --- Document parsing (analyse-page file upload) ---
python-docx==1.1.2
pypdf==5.3.0

# --- Plotting (risk-coverage curve F1, feature-distribution figures) ---
matplotlib==3.10.0
